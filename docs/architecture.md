# Architecture

The NextCloud instance runs on a Raspberry Pi 2 inside my home network.

- Static local IP assigned via router DHCP reservation
- Accessible via local network
- SSH enabled for remote management
- Database hosted locally on the same device (MariaDB)
